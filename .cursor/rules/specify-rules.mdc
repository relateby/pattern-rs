# gram-rs Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-27

## Active Technologies
- N/A (test infrastructure only, no persistent data storage) (003-test-infrastructure)
- Rust 1.70.0+ (workspace MSRV), Edition 2021 (004-pattern-data-structure)
- N/A (in-memory data structures only) (004-pattern-data-structure)
- N/A (in-memory operations only) (009-foldable-instance)
- N/A (in-memory transformations only) (010-traversable-instance)
- Rust 1.75+ (matches existing codebase) + None (core library functionality, no external dependencies needed) (011-basic-query-functions)
- N/A (pure computation) (011-basic-query-functions)
- Rust 1.70.0+ (workspace MSRV), Edition 2021 + None (core library functionality, uses std::cmp only) (012-ord-trait)
- N/A (pure computation, no persistence) (012-ord-trait)
- Rust 1.70.0+ (workspace MSRV), Edition 2021 + None (core library functionality) (013-semigroup-instance)
- Rust 1.75+ (edition 2021) + None (std library only for core functionality) (016-predicate-matching)
- N/A (pure functional data structure) (016-predicate-matching)
- Rust 1.75+ (stable features only) (018-comonad-instance)
- N/A (pure in-memory data structure operations) (018-comonad-instance)
- N/A (in-memory transformations) (019-gram-codec)
- Rust 1.70.0+ (edition 2021) (021-pure-rust-parser)
- N/A (parser library, no persistent storage) (021-pure-rust-parser)
- Rust 1.70.0+ (edition 2021) + Cargo workspace managemen (023-remove-placeholder-crates)
- N/A (no data persistence involved) (023-remove-placeholder-crates)

- Rust (MSRV: 1.70.0, edition: 2021) (002-workspace-setup)

## Project Structure

```text
src/
tests/
```

## Commands

cargo test [ONLY COMMANDS FOR ACTIVE TECHNOLOGIES][ONLY COMMANDS FOR ACTIVE TECHNOLOGIES] cargo clippy

## Code Style

Rust (MSRV: 1.70.0, edition: 2021): Follow standard conventions

## Recent Changes
- 023-remove-placeholder-crates: Added Rust 1.70.0+ (edition 2021) + Cargo workspace managemen
- 021-pure-rust-parser: Added Rust 1.70.0+ (edition 2021)
- 019-gram-codec: Added Rust 1.75+ (edition 2021)


<!-- MANUAL ADDITIONS START -->

## Gram Notation Validation

Use the `gram-lint` CLI tool to validate gram notation snippets:

**For files:**
```bash
gram-lint path/to/file.gram
```

**For inline expressions:**
```bash
gram-lint -e "your gram expression here"
```

**For stdin:**
```bash
echo "gram expression" | gram-lint
# or
gram-lint -
```

**To see parse tree:**
```bash
gram-lint -t -e "your gram expression"
```

When working with gram notation in documentation, tests, or examples, validate snippets using `gram-lint` to catch parse errors early.

<!-- MANUAL ADDITIONS END -->
